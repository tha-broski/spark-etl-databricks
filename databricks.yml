bundle:
  name: spark-etl-databricks

include:
  - resources/*.yml

artifacts:
  default:
    type: whl
    path: .
    build: python -m pip wheel . -w dist --no-deps

targets:
  dev:
    mode: development
    default: true